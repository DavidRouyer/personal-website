---
import { resolveLanguage } from '../i18n/utils';

const acceptLanguage = Astro.request.headers.get('Accept-Language') || 'en';
const resolvedLanguage = resolveLanguage(acceptLanguage);

return Astro.redirect(`/${resolvedLanguage}/`);
---